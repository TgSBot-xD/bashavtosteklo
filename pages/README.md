This empty `pages/` folder is required by Next.js when using the App Router.

Without it, Next.js will try to use `src/pages/` as the Pages Router,
which will break the build.

All actual pages are located in `src/pages/` (FSD layer).
